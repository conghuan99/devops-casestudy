apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10   # reload dashboard nếu file JSON thay đổi
    options:
      path: /var/lib/grafana/dashboards
